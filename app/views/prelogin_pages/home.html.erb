<%= provide(:title, "Home") %>
<% if user_signed_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section>
        <%= render 'shared/card_form' %>
      </section>
    </aside>
    <div class="col-md-8">
      <h3>Flashcard Feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
  <div class="center">
    <h1>Flashcard Share</h1>
    <h2>This is the home page.</h2>
  </div>
<% end %>